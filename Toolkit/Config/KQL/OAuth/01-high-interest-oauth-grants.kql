// Shadow Trace Ops - OAuth / App Consent Review
CloudAppEvents
| where Timestamp > ago({LookbackDays}d)
| where ActionType has_any ("Consent", "Add service principal", "Add delegated permission grant")
| project Timestamp, AccountDisplayName, Application, ActionType, RawEventData
| order by Timestamp desc
